@model HELPER.NET.ENTIDADES.Dto.Generico.SubRespuesta

@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "Formulario" }))
{
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-12">
                <div id="txtMensajeError" class="alert alert-danger" role="alert">
                    @Model.mensaje : @Model.mensajeDetalle
                </div>
            </div>
        </div>
    </div>
}